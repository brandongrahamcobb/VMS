FROM rust:latest
RUN apt-get update && apt-get install -y \
    pkg-config \
    libssl-dev \
    libpq-dev \
    build-essential \
    && rm -rf /var/lib/apt/lists/*
WORKDIR /app
COPY . .
CMD ["cargo", "test", "-p", "test-suite", "--", "--nocapture"]
