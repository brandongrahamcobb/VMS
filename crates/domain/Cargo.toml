[package]
name = "domain"
version = "0.1.0"
edition = "2024"

[dependencies]
thiserror = "2"
metadata = { path = "../metadata" }
entity = { path = "../entity" }
db = { path = "../db" }
assembly = { path = "../assembly" }
config = { path = "../config" }
rand = "0.10.1"
